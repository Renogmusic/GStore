@model GStore.Models.WebFormResponse
@if (Model == null)
{
	@:(no Web Form Response)
}
else
{
	@(Html.DisplayNameForModel()) 
	@:'@(Html.DisplayFor(model => model.CreateDateTimeUtc))' [@(Html.DisplayFor(model => model.WebFormResponseId))]
}
