@model Product
@{
	if (Model == null)
	{
		@:(none)
	}
	else
	{
		<div class="row CheckoutItemNameRow">
			@Model.Name
		</div>
		if (!string.IsNullOrEmpty(Model.ImageName))
		{
			<div class="row CheckoutItemImageRow">
				<img class="CheckoutItemImage" src="@(Html.Raw(Model.ImageUrl(Request.ApplicationPath, ViewContext.RouteData, true)))" />
			</div>
		}
	}
}