@model GStoreData.Models.Page
@{
	if (Model == null)
	{
		@:(none)
	}
	else
	{
		@:'@Model.Name' [@(Model.PageId)] - @(Html.DisplayNameFor(model => model.StoreFront)) @Html.DisplayFor(model => model.StoreFront)
	}
}