@model StoreFront
@if (Model == null)
{
	@:(no store front)
}
else
{
	bool isActiveDirect = Model.IsActiveDirect();
	bool isActiveBubble = Model.IsActiveBubble();
	string className = isActiveBubble ? string.Empty : "text-warning";
	<span class="@className" style="white-space:nowrap">
		@(Html.DisplayNameForModel()) '@(Model.CurrentConfigOrAny().Name)' [@(Model.StoreFrontId)] @(isActiveDirect ? string.Empty : " [INACTIVE]")
	</span>
	<br />
	@(Html.DisplayFor(model => model.Client, "ClientNoLinks"))
}

